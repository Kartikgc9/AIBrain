# Server Configuration
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# Database (PostgreSQL with pgvector)
DATABASE_URL=postgresql://aibrain:dev_password@localhost:5432/aibrain_dev

# Redis (for caching and sessions)
REDIS_URL=redis://localhost:6379

# Authentication (minimum 32 characters)
JWT_SECRET=your_secret_key_here_must_be_at_least_32_characters_long

# OpenAI API (for embeddings generation)
OPENAI_API_KEY=sk-your-api-key-here

# CORS (comma-separated origins or * for all)
CORS_ORIGIN=*
